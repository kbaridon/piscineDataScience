services:
  postgres:
    build:
      context: ./requirements/postgreSQL
    container_name: postgreSQL
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
